%h1 Drafts

%ul
  - drafts.each do |draft|
    %li
      .created= link_to(draft.title || draft.created_at.strftime("%A, %B %d, %Y"), "/#{draft.token}")
      .updated= draft.updated_at.strftime("%H:%M %p on %A")
      .edits= draft.versions.count
