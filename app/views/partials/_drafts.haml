%h1 Drafts

.draft-list
  - drafts.each do |draft|
    %a.draft(href="/#{draft.token}")
      .created= draft.title || draft.created_at.strftime("%A, %B %d, %Y")
      .updated= draft.updated_at.strftime("%H:%M %p on %A")
      .edits= draft.versions.count
